#include<stdio.h>
int main()
{
	printf("ENTER THE MAXIMUM LIMIT TO GET PRIME NUMBERS : ");
	scanf("%d",&max);
	for (i = 1 ; i <= max; i ++)
		 {
      if(max%i==0)
      {
         i++;
      }
}
   if(i==2)
   {
      printf("Entered number is %d "\
             "and it is a prime number.",);
   }
   else
   {
      printf("Entered number is %d "\
             "and it is not a prime number.",num);
   }
}

	}
	
	}
	
	
	
	
	
	}
